root@4245891-amelix:/var/www/card-game/public/js# cat /etc/nginx/sites-available/card-game
server {
    listen 443 ssl http2;
    server_name panti.bestaibots.ru;

    ssl_certificate /etc/letsencrypt/live/bestaibots.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bestaibots.ru/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
        proxy_redirect off;
    }
}